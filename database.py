import sqlite3\n\nclass Database:\n    def __init__(self, db_name='quiz_bot.db'):  # Default database name\n        self.connection = sqlite3.connect(db_name)\n        self.cursor = self.connection.cursor()\n        self.create_tables()\n\n    def create_tables(self):\n        self.cursor.execute('''CREATE TABLE IF NOT EXISTS users (id INTEGER PRIMARY KEY, username TEXT UNIQUE, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP)''')\n        self.cursor.execute('''CREATE TABLE IF NOT EXISTS quizzes (id INTEGER PRIMARY KEY, title TEXT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP)''')\n        self.cursor.execute('''CREATE TABLE IF NOT EXISTS scores (id INTEGER PRIMARY KEY, user_id INTEGER, quiz_id INTEGER, score INTEGER, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY(user_id) REFERENCES users(id), FOREIGN KEY(quiz_id) REFERENCES quizzes(id))''')\n        self.cursor.execute('''CREATE TABLE IF NOT EXISTS question_history (id INTEGER PRIMARY KEY, user_id INTEGER, question TEXT, answered_correctly BOOLEAN, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY(user_id) REFERENCES users(id))''')\n        self.connection.commit()\n\n    def create_user(self, username):\n        try:\n            self.cursor.execute('INSERT INTO users (username) VALUES (?)', (username,))\n            self.connection.commit()\n        except sqlite3.IntegrityError:\n            print('User already exists.')\n\n    def get_user(self, user_id):\n        self.cursor.execute('SELECT * FROM users WHERE id = ?', (user_id,))\n        return self.cursor.fetchone()\n\n    def update_user(self, user_id, username):\n        self.cursor.execute('UPDATE users SET username = ? WHERE id = ?', (username, user_id))\n        self.connection.commit()\n\n    def delete_user(self, user_id):\n        self.cursor.execute('DELETE FROM users WHERE id = ?', (user_id,))\n        self.connection.commit()\n\n    def create_quiz(self, title):\n        self.cursor.execute('INSERT INTO quizzes (title) VALUES (?)', (title,))\n        self.connection.commit()\n\n    def get_quiz(self, quiz_id):\n        self.cursor.execute('SELECT * FROM quizzes WHERE id = ?', (quiz_id,))\n        return self.cursor.fetchone()\n\n    def update_quiz(self, quiz_id, title):\n        self.cursor.execute('UPDATE quizzes SET title = ? WHERE id = ?', (title, quiz_id))\n        self.connection.commit()\n\n    def delete_quiz(self, quiz_id):\n        self.cursor.execute('DELETE FROM quizzes WHERE id = ?', (quiz_id,))\n        self.connection.commit()\n\n    def create_score(self, user_id, quiz_id, score):\n        self.cursor.execute('INSERT INTO scores (user_id, quiz_id, score) VALUES (?, ?, ?)', (user_id, quiz_id, score))\n        self.connection.commit()\n\n    def get_scores_for_user(self, user_id):\n        self.cursor.execute('SELECT * FROM scores WHERE user_id = ?', (user_id,))\n        return self.cursor.fetchall()\n\n    def create_question_history(self, user_id, question, answered_correctly):\n        self.cursor.execute('INSERT INTO question_history (user_id, question, answered_correctly) VALUES (?, ?, ?)', (user_id, question, answered_correctly))\n        self.connection.commit()\n\n    def close(self):\n        self.connection.close()\n\n# Example Usage:\n# db = Database()\n# db.create_user('john_doe')\n# print(db.get_user(1))\n# db.close()\n